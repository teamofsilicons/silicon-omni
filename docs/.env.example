# Optional. The dial is read from docs/data/models.json in a public GitHub repo,
# so nothing here is required to run the site.

# Where to read the model list from. OMNI_BRANCH must name a real branch —
# raw.githubusercontent does not resolve HEAD.
OMNI_REPO=teamofsilicons/silicon-omni
OMNI_BRANCH=main
